API_HOST=127.0.0.1
API_PORT=3001
DATABASE_URL=postgresql://tedixhunt:local-development-only@127.0.0.1:5432/tedixhunt
PGPOOL_MAX=10
# Existing Mongo-backed routes need this until migrated in later steps.
MONGODB_URI=
JWT_SECRET=replace-with-a-secure-secret
